import Anthropic from "@anthropic-ai/sdk"; import { createClient } from "@supabase/supabase-js"; const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY }); function supabaseAdmin() { return createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!); } interface PendingTxn { id: string; merchant_name: string | null; amount: number; raw_plaid_category: string | null; } export async function categorizePendingTransactions(clientId: string) { const supabase = supabaseAdmin(); const { data: pending, error } = await supabase.from("transactions").select("id, merchant_name, amount, raw_plaid_category").eq("client_id", clientId).eq("status", "pending_review").is("ai_category_id", null); if (error) throw error; if (!pending?.length) return; const { data: categories } = await supabase.from("categories").select("id, name, coa_code").or("client_id.eq." + clientId + ",is_default.eq.true"); const { data: rules } = await supabase.from("category_mapping_rules").select("merchant_pattern, category_id, confidence_score").eq("client_id", clientId); const ruleMap = new Map((rules ?? []).map((r: any) => [normalizeMerchant(r.merchant_pattern), r])); const needsClaude: PendingTxn[] = []; for (const txn of pending as PendingTxn[]) { const key = normalizeMerchant(txn.merchant_name ?? ""); const rule = ruleMap.get(key); if (rule && rule.confidence_score >= 0.85) { await applyCategory(supabase, txn.id, rule.category_id, rule.confidence_score); await bumpRuleUsage(supabase, clientId, key); } else { needsClaude.push(txn); } } if (needsClaude.length) { await categorizeBatchWithClaude(supabase, clientId, needsClaude, categories ?? []); } } function normalizeMerchant(name: string): string { return name.trim().toLowerCase().replace(/\\s+/g, " "); } async function applyCategory(supabase: any, transactionId: string, categoryId: string, confidence: number) { await supabase.from("transactions").update({ ai_category_id: categoryId, ai_confidence: confidence, updated_at: new Date().toISOString() }).eq("id", transactionId); } async function bumpRuleUsage(supabase: any, clientId: string, merchantKey: string) { await supabase.rpc("increment_rule_hit_count", { p_client_id: clientId, p_merchant_pattern: merchantKey }); } async function categorizeBatchWithClaude(supabase: any, clientId: string, txns: PendingTxn[], categories: any[]) { const categoryList = categories.map((c: any) => "- " + c.id + ": " + c.name + (c.coa_code ? " (" + c.coa_code + ")" : "")).join("\\n"); const txnList = txns.map((t: any, i: number) => i + ". merchant=""" + (t.merchant_name ?? "unknown") + """, amount=" + t.amount + ", plaid_category=""" + (t.raw_plaid_category ?? "none") + """").join("\\n"); const response = await anthropic.messages.create({ model: "claude-sonnet-4-6", max_tokens: 2000, system: "You are a bookkeeping categorization engine. You must choose exactly one category_id from the provided list for each transaction. Never invent a category_id. Respond ONLY with a JSON array, no prose, no markdown fences.", messages: [{ role: "user", content: "Chart of accounts:\\n" + categoryList + "\\n\\nTransactions to categorize:\\n" + txnList + "\\n\\nRespond with a JSON array like: [{\"index\": 0, \"category_id\": \"...\", \"confidence\": 0.0-1.0}]" }] }); const textBlock = response.content.find((b: any) => b.type === "text"); if (!textBlock || textBlock.type !== "text") throw new Error("No text response from Claude"); const results: any[] = JSON.parse(textBlock.text); for (const r of results) { const txn = txns[r.index]; if (!txn) continue; await applyCategory(supabase, txn.id, r.category_id, r.confidence); if (txn.merchant_name) { await supabase.from("category_mapping_rules").upsert({ client_id: clientId, merchant_pattern: normalizeMerchant(txn.merchant_name), category_id: r.category_id, confidence_score: r.confidence }, { onConflict: "client_id,merchant_pattern" }); } } } export async function recordCorrection(transactionId: string, clientId: string, fromCategoryId: string | null, toCategoryId: string, correctedBy: string) { const supabase = supabaseAdmin(); await supabase.from("category_corrections").insert({ transaction_id: transactionId, client_id: clientId, from_category_id: fromCategoryId, to_category_id: toCategoryId, corrected_by: correctedBy }); const { data: txn } = await supabase.from("transactions").select("merchant_name").eq("id", transactionId).single(); if (txn?.merchant_name) { const key = normalizeMerchant(txn.merchant_name); await supabase.from("category_mapping_rules").upsert({ client_id: clientId, merchant_pattern: key, category_id: toCategoryId, confidence_score: 0.6 }, { onConflict: "client_id,merchant_pattern" }); } await supabase.from("transactions").update({ ai_category_id: toCategoryId, status: "confirmed", updated_at: new Date().toISOString() }).eq("id", transactionId); } export async function categorizeWithLocalRules(clientId: string): Promise<{ categorized: number; skipped: number }> { const { createClient } = await import("@supabase/supabase-js"); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!, { auth: { persistSession: false } }); const { data: pending } = await supabase.from("transactions").select("id, merchant_name, raw_plaid_category").eq("client_id", clientId).eq("status", "pending_review").is("ai_category_id", null); if (!pending?.length) return { categorized: 0, skipped: 0 }; const { data: categories } = await supabase.from("categories").select("id, name").or("client_id.eq." + clientId + ",is_default.eq.true"); if (!categories?.length) return { categorized: 0, skipped: pending.length }; const rules = [{ pattern: /credit card.*payment|card.*payment/i, hints: ["credit card", "payment"] }, { pattern: /intrst|interest/i, hints: ["interest", "transfer"] }, { pattern: /aws|cloud|hosting/i, hints: ["software", "subscriptions"] }, { pattern: /office depot|supplies/i, hints: ["office"] }, { pattern: /transfer/i, hints: ["transfer"] }, { pattern: /payment.*credit card/i, hints: ["credit card", "payment"] }, { pattern: /transfer.*credit/i, hints: ["transfer", "interest"] }]; let categorized = 0, skipped = 0; for (const txn of pending) { const searchText = (txn.merchant_name ?? "") + " " + (txn.raw_plaid_category ?? ""); let matchedId: string | null = null; for (const rule of rules) { if (!rule.pattern.test(searchText)) continue; const hit = categories.find((c: any) => rule.hints.some((h: string) => c.name.toLowerCase().includes(h))); if (hit) { matchedId = hit.id; break; } } if (matchedId) { await supabase.from("transactions").update({ ai_category_id: matchedId, ai_confidence: 1.0 }).eq("id", txn.id); categorized++; } else { skipped++; } } return { categorized, skipped }; }
